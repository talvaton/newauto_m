reset_form($('input[value="<%= @ticket.form_name %>"]').closest('form'));

$.fancybox.close();

$.fancybox.open('<div><div>Ваша заявка принята</div><div>Наш менеджер свяжется с вами в ближайшее время.</div></div>', {
    afterShow: function(){

        $('input[value="<%= @ticket.form_name %>"]').closest('form').find('button:submit').prop("disabled", false);
      /*  gtag('event', 'send', {
            'event_category': 'form',
            'event_label': 'send'
        });*/

          //ADS
     //   gtag_report_conversion();


        // yandex form
        //ym(80934016,'reachGoal','AllForms'); //all form
       // yaCounter80934016.reachGoal("AllForms");
        ym(80934016,'reachGoal','AllForms');

        if ( $("body form.new_ticket").is("#expresscredithome_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm5')
        }

        if ( $("body form").is("#tradeinpage_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm659')
        }
        if ( $("body form").is("#creditpage_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm1460')
        }
        if ( $(".fancybox-stage form").is(".tradein_modal_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm6')
        }
        if ( $(".fancybox-stage form").is(".kredit_modal_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm853')
        }
        if ( $(".ex_fbn_form form").is(".spec_modal_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm664')
        }
        if ( $(".ex_fbn_form form").is(".obm_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm1026')
        }
        if ( $(".credit_fbn_form form").is(".credit_20_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm1461')
        }
        if ( $("form").is(".banks_new_ticket")) {
            ym(80934016,'reachGoal','ClientForm145744286203')
        }
        if ( $("form").is(".taxi_new_ticket")) {
            ym(80934016,'reachGoal','taxi')
        }
        if ( $(".modal_cookie_message form").find(".cookie_modal_vtb")) {
            ym(80934016,'reachGoal','popup_vtb')
        }

        // yandex form


        // цели для таргета
        var _tmr = window._tmr || (window._tmr = []);
        _tmr.push({id: "3211271", type: "reachGoal", goal: "send"});


       // var _tmr = window._tmr || (window._tmr = []); _tmr.push({ id: "3063561", type: "reachGoal", goal: "vsezayavki"})


        //console.log("<%#= @ticket.form_name %>");

//        if ("<%#= @ticket.form_name %>" === "Всплывающее окно на странице модели") {
//            setCookie('model-popup-shown', '1', 1)

//            $('.car-visible-modal').addClass('hidden')
//        }


        //fb
      //  fbq('track', 'Lead');


        var ct_site_id = 45105;
        var ct_data = {
            //fio: 'Иванов Иван Иванович',
            //phoneNumber: '79000000000',
            //email: 'test@test.ru',
            subject: '<%= @ticket.form_name %>',
            //tags: 'Mercedes,Белый',
            // comment: 'Тест-драйв',
            sessionId: window.ct('calltracking_params','mod_id').sessionId
        };
        jQuery.ajax({
            url: 'https://api.calltouch.ru/calls-service/RestAPI/requests/45105/register/',
            dataType: 'json',
            type: 'POST',
            data: ct_data
        });




    }
});

$('[data-role="ny-dialog"]').fadeOut(200);
$('.wrapper').removeClass('wrapper--fixed');

//$.jGrowl("Наш менеджер свяжется с вами в ближайшее время.", {
//    theme: "af-message-success",
//    sticky: true,
//    position: "center",
//    pool: 1,
//    header: "Ваша заявка принята",
//    life: 150000
//})