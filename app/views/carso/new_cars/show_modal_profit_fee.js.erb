$.fancybox.open('<%= escape_javascript (render partial:'new_cars/modal_profit_fee', :object => @car) %>',
    {
        touch: false,
        animationDuration: 500,
        animationEffect: "fade",
        afterLoad: function(){
            $('#profit__fee').css('display', 'inline-block');
        },
        afterShow: function(){
            DISABLE_FANCYBOX = false;
        }
    }
);