$.fancybox.open('<%= escape_javascript (render partial:'new_cars/modal_profit_credit', :object => @car) %>',
    {
        touch: false,
        animationDuration: 500,
        animationEffect: "fade",
        afterLoad: function(){
            $('#profit__credit').css('display', 'inline-block');
        },
        afterShow: function(){
            DISABLE_FANCYBOX = false;
        }
    }
);