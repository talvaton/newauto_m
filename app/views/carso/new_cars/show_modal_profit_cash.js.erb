$.fancybox.open('<%= escape_javascript (render partial:'new_cars/modal_profit_cash', :object => @car) %>',
    {
        touch: false,
        animationDuration: 500,
        animationEffect: "fade",
        afterLoad: function(){
            $('#profit__cash').css('display', 'inline-block');
        },
        afterShow: function(){
            DISABLE_FANCYBOX = false;
        }
    }
);