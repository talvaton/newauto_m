$.fancybox.open('<%= escape_javascript (render partial:'new_cars/modal_details', :object => @car) %>',
    {
        touch: false,
        animationDuration: 500,
        animationEffect: "fade",
        afterLoad: function(){
            $('#car-details-modal').css('display', 'inline-block');
            init_labels('#car-details-modal .input, #car-details-modal .select');
        },
        afterShow: function(){
            disableFormBtn();
            $('#new_details_modal_new_ticket').enableClientSideValidations();
            init_phone_mask();
            $('#new_details_modal_new_ticket .input, .select').on('focus', function(){
                $(this).removeClass('input--error');
                $(this).siblings('.form__error').slideUp();
            });
            DISABLE_FANCYBOX = false;
        }
    }
);