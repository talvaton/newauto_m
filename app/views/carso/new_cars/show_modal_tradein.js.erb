$.fancybox.open('<%= escape_javascript (render partial:'new_cars/modal_tradein', :object => @car) %>',
    {
        touch: false,
        animationDuration: 500,
        animationEffect: "fade",
        afterLoad: function(){
            $('#car-tradein-modal').css('display', 'inline-block');
            init_labels('#car-tradein-modal .input, #car-tradein-modal .select');
        },
        afterShow: function(){
            disableFormBtn();
            $('#new_tradein_modal_new_ticket').enableClientSideValidations();
            init_phone_mask();
            $('#new_tradein_modal_new_ticket .input, .select').on('focus', function(){
                $(this).removeClass('input--error');
                $(this).siblings('.form__error').slideUp();
            });
            DISABLE_FANCYBOX = false;
        }
    }
);