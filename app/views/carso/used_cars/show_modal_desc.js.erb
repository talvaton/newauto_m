$.fancybox.open('<%= escape_javascript (render partial:'used_cars/modal_desc', :object => @car) %>',
    {
        touch: false,
        animationDuration: 500,
        animationEffect: "fade",
        afterLoad: function(){
            $('#used-car-details-modal').css('display', 'inline-block');
//            $('.scrollbar-outer').scrollbar();
            init_labels('#used-car-details-modal .input, #used-car-details-modal .select');
        },
        afterShow: function(){
            disableFormBtn();
            $('#desc_modal_new_ticket').enableClientSideValidations();
            init_phone_mask();
            $('#desc_modal_new_ticket .input, .select').on('focus', function(){
                $(this).removeClass('input--error');
                $(this).siblings('.form__error').slideUp();
            });
            DISABLE_FANCYBOX = false;

        }
    }
);